Passwords are stored in lastpass under predictable names.

Connect as admin:

sh postgresql/admin-connection

Connect as inflex:

sh postgresql/inflex-connection

Setup a fresh database with the inflex user:

postgresql/database-setup
